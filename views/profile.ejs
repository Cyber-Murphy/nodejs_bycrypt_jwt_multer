<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end"> 

            <a class="inline-block bg-red-500 rounded-md px-3 py-2 text-sm mb-5" href="/logout">LogOut</a>
        </div>
        <div class="flex items-start gap-3">
                <div class="w-10 h-10  rounded-md">
                    <img class="w-full h-full object-cover" src="'public/images/uploads<%= user.profilepic %>" alt="">
                </div>
            <h3 class="text-3xl mb-3">Hello ,<%= user.name %></h3>
        </div>
        <h3 class="mb-5 mt-2">You can create a new post</h3>
        <form action="/post" method="post">
                <textarea class=" block p-3 w-1/3 resize-none bg-transparent border-2 bg-zinc-800 rounded-md"  placeholder="what going in your mind?" name="content" id=""></textarea>
                <input type="submit" class=" w-40 block mt-2 px-2 py-3  bg-blue-500 rounded-md" name="Create New Post">

        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="post w-1/3 mt-2 p-4 rounded-md border-2 border-zinc-800 bg-zinc-800">
                        <h4 class="text-blue-500 mb-2">@<%= user.Username %></h4>
                        <p class=" text-sm tracking-tight"><%= post.content %></p>
                        <small class="mt-2 inline-block"><%= post.likes.length %>likes</small>
                        <div class="btns flex gap-4 mt-5">
                            <a class ="text-blue-500"href="/like/<%= post._id %>">
                                <%= post.likes.indexOf(user._id)==-1 ? "Like":"Unlike" %></a>
                            <a class ="text-zinc-500"href="">Edit</a>
                        </div>

                    </div>
                <%}) %>
                    
                    <div class="post w-1/3 mt-2 p-4 rounded-md border-2 border-zinc-800 bg-zinc-800">
                        <h4 class="text-blue-500 mb-2">@someusername</h4>
                        <p class=" text-sm tracking-tight">Lorem ipsum dolor sit amet consectetur, adipisicing elit. Asperiores quis veniam quae minus, maiores et?</p>
                        <div class="btns flex gap-4 mt-5">
                            <a class ="text-blue-500"href="">Like</a>
                            <a class ="text-zinc-500"href="">Edit</a>
                        </div>

                    </div>

            </div>

        </div>
        


    </div>
    
</body>
</html>